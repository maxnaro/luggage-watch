\section{Testing and Evaluation}
\label{sec:testing_and_evaluation}

\subsection{Object Detection and Tracking Performance}
\label{sec:object_detection_and_tracking_performance}

\paragraph{Object detection performance.}

Object detection metrics were evaluated using the held-out test split of the
ABODA dataset, which was not included in the training data. Note that the
training and test splits originate from the same dataset, so the model has been
exposed to the same visual domain during fine-tuning
(section~\ref{sec:training_process}), though not the specific test images. The
performance of the YOLO26 model was assessed using standard object detection
metrics, including mean Average Precision (mAP) at various Intersection over
Union (IoU) thresholds.

The test split comprised $111$ images containing $202$ annotated instances
($99$ person, $103$ luggage). Table~\ref{tab:detection_results} summarises the
per-class results.

\begin{table*}[!htb]
	\centering
	\small
	\begin{tabularx}{\textwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X}
		\toprule
		\textbf{Class} & \textbf{Precision} & \textbf{Recall} & \textbf{mAP@0.5} & \textbf{mAP@0.5:0.95} \\
		\midrule
		Person         & 0.884              & 0.788           & 0.852            & 0.438                 \\
		\addlinespace
		Luggage        & 0.898              & 0.845           & 0.867            & 0.434                 \\
		\midrule
		All            & 0.891              & 0.816           & 0.859            & 0.436                 \\
		\bottomrule
	\end{tabularx}
	\caption{Object detection results on the ABODA test split.}
	\label{tab:detection_results}
\end{table*}

The model achieved an overall mAP@0.5 of $0.859$, with both classes performing
comparably. Luggage detection slightly outperformed person detection in both
precision ($0.898$ vs.\ $0.884$) and recall ($0.845$ vs.\ $0.788$), suggesting
the fine-tuning phase successfully adapted the model to luggage instances in
surveillance contexts.

The precision-recall curve (figure~\ref{fig:pr_curve}) shows that both classes
maintain high precision ($>0.9$) up to approximately $0.6$ recall before
degrading. The confusion matrix (figure~\ref{fig:confusion_matrix}) reveals
that inter-class confusion is minimal, only $2\%$ of true luggage instances are
misclassified as person. The primary source of error is background false
positives, where $55\%$ of background regions trigger person predictions and
$45\%$ trigger luggage predictions. In the context of the downstream pipeline,
these false positives are mitigated by the tracker's temporal consistency
requirements, which filter out transient spurious detections.

The relatively small test set ($111$ images) means these metrics should be
interpreted with caution, as individual images can disproportionately influence
the results.

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/object_detection/BoxPR_curve.png}
	\caption{Precision-recall curve for person and luggage classes on the ABODA test split.}
	\label{fig:pr_curve}
\end{figure}

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/object_detection/confusion_matrix_normalized.png}
	\caption{Normalised confusion matrix showing per-class classification accuracy and background false positive rates.}
	\label{fig:confusion_matrix}
\end{figure}

\paragraph{Limitations of object tracking evaluation.}

The object tracking component of the pipeline was unable to be quantitatively
evaluated due to the absence of frame-level annotations for the target domain
test set. The idea of running semi-automated annotation for a MOTA-style ground
truth using the object detection model was tested but ultimately abandoned due
to the infeasible amount of manual correction required to produce a reliable
ground truth. As a result, the evaluation of the tracking component was limited
to the context of the pipeline as a whole, with the focus on the abandonment
logic over the tracking performance in isolation.

\subsection{Validation Protocol for Abandonment Logic}

Due to the unavailability of frame-level annotations for the test set, a
temporal event-based ground truth was established. An event was defined by a
single temporal anchor (the frame of abandonment) and a spatial region of
interest (ROI). The pipeline was evaluated using a temporal tolerance of $\pm
	360$ frames ($\approx 12$ seconds depending on the frame rate) to account for
manual annotation variability and the system's hysteresis.

\subsubsection{Ground Truth Annotation Methodology}
\label{sec:ground_truth_annotation_methodology}

Due to the temporal nature of abandoned luggage, traditional frame-by-frame
bounding box annotations were impractical. Instead, the ground truth was
established using a single-point event anchoring method.

For each video in the dataset, the annotation process followed these steps:

\begin{enumerate}
	\item \textbf{Scene Triage and Parameter Definition}
	      \begin{itemize}
		      \item Reviewal of the entire video sequence to determine if a genuine abandonment
		            event has occurred, setting the \texttt{has\_abandonment} flag accordingly.
		      \item Based on the camera's perspective, focal length, and requirements of the
		            scenario, definition of the spatial and temporal limits for that specific
		            video; i.e., determining the radius of ownership and the temporal window for
		            abandonment detection as \texttt{radius\_px} and \texttt{threshold\_s}
		            respectively.
	      \end{itemize}
	\item \textbf{Event Anchoring}
	      \begin{itemize}
		      \item If an abandonment event is identified, the annotator scrubs through the video
		            to identify the exact moment the bag transitions from being unattended to being
		            abandoned.
		      \item Calculate the frame number corresponding to this moment, by determining where
		            the owner breaches the ownership radius, and then moving forward in time by the
		            defined threshold to anchor the event.
		      \item This frame number is then recorded as the \texttt{true\_abandon\_frame}. This
		            metric acts as the temporal anchor against which the pipeline's detection
		            latency is measured.
		      \item If there are multiple events that occur in the same video, multiple anchors are
		            committed to the ground truth, with the spatial parameters remaining the same.
		            This allows for the evaluation of more complex sequences.
	      \end{itemize}
	\item \textbf{Negative Sample Logging}
	      \begin{itemize}
		      \item If the video contains no abandonment event (e.g., a bag is left unattended but
		            the owner returns within the defined temporal threshold), the video is tagged
		            with \texttt{has\_abandonment} set to \texttt{false}. The frame and ROI fields
		            are left null.
		      \item Negative samples are useful for testing the pipeline's resilience against false
		            positives.
	      \end{itemize}
\end{enumerate}

Annotations are saved in a structured \texttt{JSON} format
(listing~\ref{lst:annotation_format}), producing a comprehensive ground truth
dataset that is strictly tied to the logic of the abandonment detection
pipeline.

\subsubsection{Annotation Script}
\label{sec:annotation_script}

To facilitate the annotation process, a custom script was developed. This
script provided a user-friendly interface for annotators to review video
footage, define spatial and temporal parameters and record the necessary
information for each video. This was implemented using Python and OpenCV,
allowing for video playback control and scrubbing. An example of the script's
interface is shown in figure~\ref{fig:annotation_script_screenshot}.

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/annotation_script_screenshot.png}
	\caption{Screenshot of the annotation script interface, showing video playback controls and parameter input fields.}
	\label{fig:annotation_script_screenshot}
\end{figure}

\subsubsection{Datasets Used for Validation}

Annotations were performed on the eleven examples from the ABODA dataset
mentioned in section~\ref{sec:suspicious_luggage_dataset}. These examples, the
ground truth annotations derived from them, and the annotation script
(section~\ref{sec:annotation_script}) can be found in a forked repository at
\url{https://github.com/maxnaro/ABODA}. The resulting annotated dataset
consisted of eleven samples with one or more abandonment events. The IITP20
dataset's $55$ video examples, the singular AVSS example and their annotations
are not included in the repository in order to respect the original datasets'
licensing terms but were also used in the evaluation process.

\paragraph{Limitation: training and evaluation domain overlap.}

Because ABODA frames were included in the fine-tuning training data
(section~\ref{sec:training_process}), the end-to-end pipeline evaluation on
ABODA video sequences is not fully independent of the training domain. While
the object detection test split was held out from training, the pipeline-level
evaluation uses the original ABODA videos, which contain frames the detector
may have seen during training. This overlap means that pipeline results on
ABODA may overstate the system's generalisation to truly unseen surveillance
footage. The IITP20 and AVSS results, where no training overlap exists, provide
a more reliable measure of out-of-domain performance.

\subsubsection{Evaluation Script}

With an established ground truth, an evaluation script was developed to run the
pipeline, directly on the deployment hardware, against the test examples and
compare the pipeline's abandonment detections with the ground truth.

An \texttt{EventLogger} class was implemented to capture discrete abandonment
events from the pipeline's output; detected events are matched against the
ground truth event using the $\pm 360$ frame temporal tolerance to classify
detections as TP, FP, FN, or TN (see
section~\ref{sec:evaluation_metrics_for_object_detection},
table~\ref{tab:evaluation_metrics_desc}). The resulting logging output
comprising the number of events per class, frame error and IoU per video.

During processing, the script takes periodic snapshots of the system's resource
usage via the \texttt{jtop} utility. This allows for the evaluation of the
pipeline's computational efficiency while processing the test videos, providing
insight into the system's performance under load.

Once the script has processed all test videos, the logged events are aggregated
to produce performance metrics for the pipeline. The metrics include:
precision, recall, and F1-score at the event level --- mean IoU and mean frame
error for the run as a whole; metrics are calculated using the formulae
outlined in section~\ref{sec:evaluation_metrics_for_object_detection},
table~\ref{tab:evaluation_metrics_formula_1}.

\paragraph{Limitations.}

The evaluation script is designed to be run on the deployment hardware,
providing a realistic assessment of the pipeline's performance in its intended
environment. However, it is limited by the fact that it can only evaluate the
pipeline on the specific test videos for which ground truth annotations have
been established. Additionally, the script's reliance on temporal event
matching means that it may not capture nuances in detection timing or spatial
accuracy beyond the defined tolerance thresholds.

Evaluations were performed on hardware without the display sink component of
the pipeline active. This significantly reduces the time taken to run the
pipeline (as videos can be processed at a much faster rate than real-time) but
means that the evaluation results do not account for any performance impact of
the display sink, which is a component of the deployed system. Anecdotally, the
display sink was observed to have a neg.ligible impact on the pipeline's
performance, not compromising real-time performance, but this was not formally
evaluated.

\subsection{Evaluation Results}

\subsubsection[ABODA and AVSS Examples]{ABODA \protect\citep{lin2015aboda} and AVSS Examples}

\paragraph{ABODA.}

The $11$ ABODA sequences ($15$ annotated events) are summarised in
table~\ref{tab:eval_aboda_summary}; per-video results are given in
table~\ref{tab:eval_aboda} (appendix).

\begin{table}[!htb]
	\centering
	\small
	\begin{tabularx}{\columnwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}X r}
		\toprule
		\textbf{Metric}              & \textbf{Value} \\
		\midrule
		Videos                       & 11             \\
		\addlinespace
		Events (positive / negative) & 15 / 0         \\
		\addlinespace
		TP / FP / FN / TN            & 12 / 4 / 3 / 0 \\
		\addlinespace
		Precision                    & 0.750          \\
		\addlinespace
		Recall                       & 0.800          \\
		\addlinespace
		F1                           & 0.774          \\
		\addlinespace
		Mean frame error             & 197.9          \\
		\addlinespace
		Mean IoU                     & 0.627          \\
		\bottomrule
	\end{tabularx}
	\caption{Pipeline evaluation summary on the ABODA dataset.}
	\label{tab:eval_aboda_summary}
\end{table}

Expectedly, the pipeline performed the best on the ABODA examples, given the
training domain overlap (section~\ref{sec:training_process}). The majority of
video examples exhibited no false alarms on the background, with the logic
filtering noise well in the absence of any confounding factors.

The main sources of error were: ``video2'' (figure~\ref{fig:aboda_video2}),
which features a steep overhead angle, and ``video11''
(figure~\ref{fig:aboda_video11}), which features a crowded scene with a
far-away camera angle. ``video11'' is the main outlier of the dataset with no
other examples matching its level of complexity, and demonstrated that the
pipeline's logic is not robust enough to handle crowded scenes with significant
occlusion and smaller detection sizes. ``video2'' saw the model struggle to
detect the ownership consistently, with the tracking component losing the owner
and misidentifying another person as the owner, which caused the abandonment
event to be missed. This coupled with the abandonment event occurring late in
the video (figure~\ref{fig:aboda_video2_end}), which led to a much stricter
temporal requirement for the detection, meant that the event was not detected.
The two examples demonstrate the limitations of the pipeline's logic from a
detection and tracking perspective, where logic is unable to compensate for the
ReID model's lack of domain generalisation in the case of ``video2'', and the
inference model's performance limitations in crowded scenes in the case of
``video11''.

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/aboda/video11.jpg}
	\caption{Example of a crowded scene (``video11'') from the ABODA dataset.}
	\label{fig:aboda_video11}
\end{figure}

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/aboda/video2/initial.jpg}
	\caption{Example of a steep overhead angle (``video2'') from the ABODA dataset. This frame shows the initial abandonment event.}
	\label{fig:aboda_video2}
\end{figure}

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/aboda/video2/end.jpg}
	\caption{Example of a steep overhead angle (``video2'') from the ABODA dataset. This frame shows the end of the video stream, $~17$ seconds after the initial abandonment event (figure~\ref{fig:aboda_video2}).}
	\label{fig:aboda_video2_end}
\end{figure}

A greater amount of overhead examples and crowded scenes would be required in
the training data to improve the system's performance in these cases.
Aggressive data augmentation to simulate these conditions
(section~\ref{sec:training_process}) was utilised during training, but this was
not sufficient to close the gap in performance and the latter phase of training
(where augmentation was reduced) may have caused some performance degradation
in higher complexity scenarios rather than fine tuning.

Metrics were generally strong across the ABODA examples, with a high precision
of $0.750$ and recall of $0.800$ at the event level. The mean frame error of
$197.9$ frames ($\approx 6.6$ seconds) is higher than ideal, but this is
influenced by the fact that some events were detected significantly later than
the ground truth anchor, which may be due to the system's hysteresis.

Overall, the ABODA evaluation demonstrates the strongest results for the
pipeline with an F1-score of $0.774$, but these results are not fully
representative of the pipeline's performance on unseen data.

\paragraph{AVSS.}

Only a single AVSS video sequence was available for evaluation
(table~\ref{tab:eval_avss_summary}).

\begin{table}[!htb]
	\centering
	\small
	\begin{tabularx}{\columnwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}X r}
		\toprule
		\textbf{Metric}              & \textbf{Value} \\
		\midrule
		Videos                       & 1              \\
		\addlinespace
		Events (positive / negative) & 1 / 0          \\
		\addlinespace
		TP / FP / FN / TN            & 1 / 1 / 0 / 0  \\
		\addlinespace
		Precision                    & 0.500          \\
		\addlinespace
		Recall                       & 1.000          \\
		\addlinespace
		F1                           & 0.667          \\
		\addlinespace
		Mean frame error             & 23.0           \\
		\addlinespace
		Mean IoU                     & 0.635          \\
		\bottomrule
	\end{tabularx}
	\caption{Pipeline evaluation summary on the AVSS example.}
	\label{tab:eval_avss_summary}
\end{table}

The results from the single AVSS example are encouraging but inconclusive due
to the limited sample size. The single detection was temporally accurate with
tight spatial localisation, but the presence of the later false positive
indicates that the system still generates spurious alerts. Reviewing the test
run, this could be attributed to heavy occlusion at the end of the video,
causing luggage context to be transferred to a new ID yet still triggering the
abandonment logic. This highlights a potential failure mode of the pipeline in
crowded scenes with occlusion, which is consistent with the ABODA evaluation
results.

Despite the limited sample size, the AVSS example provides useful insight into
the pipeline's performance on an unseen domain, where a genuine abandonment
event was successfully detected.

\subsubsection[IITP20 Examples]{IITP20 Examples \protect\citep{amin2021large}}

\paragraph{Easy cases.}

The easy subset comprises simple scenarios with few people and the object in
full view, but includes cases where objects are placed on chairs or elevated
platforms, and videos exhibit radial distortion \citep{amin2021large}. The
subset ($17$ videos, $12$ positive and $5$ negative samples) is summarised in
table~\ref{tab:eval_iitp20_easy_summary}; per-video results are given in
table~\ref{tab:eval_iitp20_easy} (appendix).

\begin{table}[!htb]
	\centering
	\small
	\begin{tabularx}{\columnwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}X r}
		\toprule
		\textbf{Metric}              & \textbf{Value} \\
		\midrule
		Videos                       & 17             \\
		\addlinespace
		Events (positive / negative) & 12 / 5         \\
		\addlinespace
		TP / FP / FN / TN            & 6 / 19 / 6 / 5 \\
		\addlinespace
		Precision                    & 0.240          \\
		\addlinespace
		Recall                       & 0.500          \\
		\addlinespace
		F1                           & 0.324          \\
		\addlinespace
		Mean frame error             & 94.0           \\
		\addlinespace
		Mean IoU                     & 0.665          \\
		\bottomrule
	\end{tabularx}
	\caption{Pipeline evaluation summary on IITP20 easy cases.}
	\label{tab:eval_iitp20_easy_summary}
\end{table}

Precision and recall were significantly lower on the IITP20 examples compared
to ABODA, with the ``easy'' subset achieving an F1-score of only $0.324$. All 5
negative samples triggered false positives, indicating that the system's logic
is prone to hallucinations in the presence of background clutter and noise;
this is consistent with the object detection results in
section~\ref{sec:object_detection_and_tracking_performance},
figure~\ref{fig:confusion_matrix} --- which showed a significant number of
false positives in the background.

A recurring source of false positives in the IITP20 dataset is the presence of
a shelving unit storing luggage (figure~\ref{fig:iitp20_vid_00045_cluttered}).
The system was not designed with pre-existing ownerless luggage in mind: when a
person passes near the shelves, the pipeline assigns them as the owner of the
stored luggage, and their subsequent departure triggers a false abandonment
event. This failure mode is distinct from detector noise --- the detections
themselves are correct, but the ownership assignment logic has no mechanism to
distinguish luggage that was already stationary before any person entered the
scene. Despite the ``easy'' label, these environmental factors make the subset
significantly more challenging than the ABODA examples. For examples such as
figure~\ref{fig:iitp20_vid_00043_cluttered}, the system's logic is able to
correctly assign ownership of the stored luggage as null for the beginning of
the video, which allows the system to correctly identify the abandonment event
(according to the logic in section~\ref{sec:abandonment_guards}) when a bag is
left unattended in front of the shelves. However, this is not consistent across
all examples in the subsets (figure~\ref{fig:iitp20_vid_00056_non-cluttered}),
and the presence of cluttered luggage shelving units is a significant
confounding factor for the pipeline's logic.

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/iitp20/vid_00045_cluttered.jpg}
	\caption{Hallway scene (``vid\_00045'') from the IITP20 dataset featuring a luggage shelving unit. Stored luggage is incorrectly assigned ownership when a person passes nearby, leading to false abandonment alerts.}
	\label{fig:iitp20_vid_00045_cluttered}
\end{figure}

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/iitp20/vid_00043_cluttered.jpg}
	\caption{Hallway scene (``vid\_00043'') from the IITP20 dataset featuring a luggage shelving unit. Stored luggage remains ownerless for the beginning of the video, allowing the logic of the system to hold.}
	\label{fig:iitp20_vid_00043_cluttered}
\end{figure}

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/iitp20/vid_00056_non-cluttered.jpg}
	\caption{Hallway scene (``vid\_00056'') from the IITP20 dataset featuring an empty luggage shelving unit.}
	\label{fig:iitp20_vid_00056_non-cluttered}
\end{figure}

\paragraph{Medium cases.}

The medium subset adds group scenarios and person re-entry cases to the
complexity of the easy class \citep{amin2021large}. Person re-entry is
particularly relevant as the tracker may assign a new ID to a returning owner,
disrupting ownership continuity. The subset ($19$ videos, $14$ positive and $5$
negative samples) is summarised in table~\ref{tab:eval_iitp20_medium_summary};
per-video results are given in table~\ref{tab:eval_iitp20_medium} (appendix).

\begin{table}[!htb]
	\centering
	\small
	\begin{tabularx}{\columnwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}X r}
		\toprule
		\textbf{Metric}              & \textbf{Value}  \\
		\midrule
		Videos                       & 19              \\
		\addlinespace
		Events (positive / negative) & 14 / 5          \\
		\addlinespace
		TP / FP / FN / TN            & 9 / 40 / 10 / 5 \\
		\addlinespace
		Precision                    & 0.184           \\
		\addlinespace
		Recall                       & 0.474           \\
		\addlinespace
		F1                           & 0.265           \\
		\addlinespace
		Mean frame error             & 136.7           \\
		\addlinespace
		Mean IoU                     & 0.591           \\
		\bottomrule
	\end{tabularx}
	\caption{Pipeline evaluation summary on IITP20 medium cases.}
	\label{tab:eval_iitp20_medium_summary}
\end{table}

The ``medium'' subset of the IITP20 examples demonstrated the worst false alarm
rate out of any subset, with $40$ false positives across $19$ videos, leading
to a precision of only $0.184$. The presence of cluttered shelving units is
again a significant confounding factor, with many false positives triggered by
the logic misassigning ownership of stored luggage to passing pedestrians most
predominantly in ``vid\_00002'' which generated 6 false alarms on a negative
sample.

The recall of $0.474$ is consistent with the ``easy'' subset, indicating that
the system's ability to detect genuine abandonment events is not significantly
impacted by the increased complexity, but the precision degradation indicates
that the system's logic is not robust enough to handle the increased noise and
clutter in the environment. Buried within the false positives are some genuine
strong detections such as in ``vid\_00048'' where the TP has an IoU of $0.919$
but $4$ false alarms alongside it.

A concern is found in ``vid\_00052''
(figure~\ref{fig:iitp20_vid_00052_zero_iou}) where an event is detected at the
correct time, but with an IoU of $0.000$, indicating that either the
localisation is completely wrong, or the event is being triggered by a
different bag in the scene.

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/iitp20/vid_00052_zero_iou.jpg}
	\caption{Hallway scene (``vid\_00052'') from the IITP20 dataset featuring multiple luggage items where abandonment could be conflated.}
	\label{fig:iitp20_vid_00052_zero_iou}
\end{figure}

\paragraph{Hard cases.}

The hard subset introduces hidden or partially visible objects, object exchange
between people, multiple owners for a single object, and single owners for
multiple objects \citep{amin2021large}. These scenarios directly stress the
pipeline's ownership assignment logic. The subset ($19$ videos, $14$ positive
and $5$ negative samples) is summarised in
table~\ref{tab:eval_iitp20_hard_summary}; per-video results are given in
table~\ref{tab:eval_iitp20_hard} (appendix).

\begin{table}[!htb]
	\centering
	\small
	\begin{tabularx}{\columnwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}X r}
		\toprule
		\textbf{Metric}              & \textbf{Value}  \\
		\midrule
		Videos                       & 19              \\
		\addlinespace
		Events (positive / negative) & 14 / 5          \\
		\addlinespace
		TP / FP / FN / TN            & 9 / 24 / 10 / 5 \\
		\addlinespace
		Precision                    & 0.273           \\
		\addlinespace
		Recall                       & 0.474           \\
		\addlinespace
		F1                           & 0.346           \\
		\addlinespace
		Mean frame error             & 89.1            \\
		\addlinespace
		Mean IoU                     & 0.415           \\
		\bottomrule
	\end{tabularx}
	\caption{Pipeline evaluation summary on IITP20 hard cases.}
	\label{tab:eval_iitp20_hard_summary}
\end{table}

Counterintuitively, the ``hard'' subset of the IITP20 examples exhibited the
best results of the three subsets, with an F1-score of $0.346$ compared to
$0.265$ for the ``medium'' subset and $0.324$ for the ``easy'' subset. The
precision of $0.273$ is higher than the ``medium'' subset, though still
significantly lower than the ABODA examples. This is due to the fact that the
``hard'' subset contain no examples with the shelving unit confounding factor,
which is the main source of false positives in the IITP20 dataset. The recall
of $0.474$ is consistent with the other two subsets.

The main confounding factor in the ``hard'' subset is the presence of multiple
people in the scene where, with moderate to heavy occlusion, the detection and
tracking modules struggle to maintain consistent ownership assignment by way of
conjoined bounding boxes and ID switching, which leads to both false positives
and false negatives. This is consistent with the ABODA evaluation results,
where the crowded scene of ``video11'' was the main source of error. This
factor caused $5$ videos to be missed entirely (i.e., all events in the video
were missed) where luggage was detected as attended when its true owner had
left the scene (figure~\ref{fig:iitp20_vid_00058_occlusion}).

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/iitp20/vid_00058_occlusion.jpg}
	\caption{Hallway scene (``vid\_00058'') from the IITP20 dataset featuring multiple people and luggage items, and occlusion. The system fails to maintain consistent ownership assignment due to ID switching, leading to missed detections.}
	\label{fig:iitp20_vid_00058_occlusion}
\end{figure}

Similarly to the ``medium'' subset, an event was detected at the correct time
but with an IoU of $0.000$ in ``vid\_00069''
(figure~\ref{fig:iitp20_vid_00069_zero_iou}), which again indicates that the
event is being triggered by a different bag in the scene, or that the
localisation is completely wrong. This is likely due to the same ID switching
issue as ``vid\_00052'' in the ``medium'' subset, where the tracker loses the
owner and assigns a new ID to them, which causes the abandonment event to be
triggered by a different bag that has been misassigned ownership.

\begin{figure}[!htb]
	\centering
	\includegraphics[width=\columnwidth]{figures/iitp20/vid_00069_zero_iou.jpg}
	\caption{Hallway scene (``vid\_00069'') from the IITP20 dataset featuring multiple luggage items where abandonment could be conflated.}
	\label{fig:iitp20_vid_00069_zero_iou}
\end{figure}

$4$ detections occurred outside the temporal tolerance of $\pm 360$ frames, implying that
the system tends to detect later rather than not at all. In the context of the target application,
a false negative (missed detection) is more critical than a late detection, so
this is a preferable failure mode. However, the mean frame error of $89.1$ frames
($\approx 3$ seconds) indicates that there is still room for improvement in the system's
temporal accuracy.

\subsubsection{Summary of Pipeline Evaluation}

Across all datasets, precision was consistently the weakest metric, ranging
from $0.184$ (IITP20 medium) to $0.750$ (ABODA). Recall was more stable at
$0.474$--$0.800$, indicating that the system's ability to detect genuine events
does not degrade as sharply as its ability to suppress false alarms. The
pipeline over-triggers rather than under-detects.

Three recurring failure modes emerged:

\begin{enumerate}
	\item \textbf{Pre-existing ownerless luggage.} Stationary luggage present
	      before any person enters the scene (e.g., shelving units in IITP20) is
	      incorrectly assigned an owner when a person passes nearby, generating
	      false abandonment alerts on departure. This was the dominant source of
	      false positives in the IITP20 easy and medium subsets.
	\item \textbf{Tracker ID switching.} In crowded scenes with occlusion, the
	      tracker reassigns object IDs, causing ownership to transfer to the wrong
	      person or object. This manifests as zero-IoU true positives, events
	      detected at the correct time but attributed to the wrong tracked object,
	      observed across IITP20 medium (``vid\_00052'') and hard
	      (``vid\_00025'', ``vid\_00067'', ``vid\_00069'') subsets. This is a
	      tracker limitation rather than a detection failure.
	\item \textbf{Multi-event re-triggering.} In videos containing multiple
	      abandonment events, the pipeline reliably detects the first event but
	      struggles to reset and re-trigger for subsequent events. This was
	      observed in ABODA (videos 7, 8, 10) and across IITP20 multi-event
	      sequences.
\end{enumerate}

The gap between ABODA (F1$=0.774$) and IITP20 (F1$=0.265$--$0.346$) confirms
that the training domain overlap inflates the ABODA results, and that the
IITP20 evaluation provides a more realistic measure of the system's
generalisation capability. The AVSS result, while limited to a single example,
corroborates the system's ability to detect abandonment in an unseen domain.

\subsection{Hardware Utilisation}

Hardware metrics were sampled at $1$-second intervals throughout each
evaluation run using the \texttt{jtop} utility on the Jetson Orin Nano. No
thermal throttling was observed during any run (thermal threshold:
$85\degree\mathrm{C}$). Table~\ref{tab:hardware_summary} summarises the
resource utilisation across all five evaluation runs.

\begin{table*}[!htb]
	\centering
	\small
	\begin{tabularx}{\textwidth}{@{\extracolsep{\fill}}>{\raggedright\arraybackslash}X >{\centering\arraybackslash}X >{\centering\arraybackslash}X >{\centering\arraybackslash}X >{\centering\arraybackslash}X >{\centering\arraybackslash}X}
		\toprule
		\textbf{Metric}             & \textbf{ABODA}    & \textbf{AVSS}     & \textbf{IITP20 Easy} & \textbf{IITP20 Medium} & \textbf{IITP20 Hard} \\
		\midrule
		Duration                    & 6m 21s            & 54s               & 22m 19s              & 41m 20s                & 22m 35s              \\
		\addlinespace
		GPU utilisation (avg / max) & 90 / 98\%         & 89 / 98\%         & 87 / 100\%           & 90 / 100\%             & 86 / 99\%            \\
		\addlinespace
		CPU utilisation (avg / max) & 29 / 37\%         & 28 / 48\%         & 29 / 58\%            & 28 / 49\%              & 29 / 55\%            \\
		\addlinespace
		RAM utilisation (avg / max) & 70 / 73\%         & 65 / 66\%         & 73 / 77\%            & 73 / 75\%              & 72 / 75\%            \\
		\addlinespace
		Temperature (avg / max)     & 63 / 64$\degree$C & 55 / 58$\degree$C & 62 / 64$\degree$C    & 64 / 65$\degree$C      & 63 / 65$\degree$C    \\
		\addlinespace
		Thermal throttling          & None              & None              & None                 & None                   & None                 \\
		\bottomrule
	\end{tabularx}
	\caption{Hardware utilisation during evaluation runs on the NVIDIA Jetson Orin Nano (headless mode, no display sink).}
	\label{tab:hardware_summary}
\end{table*}

The GPU was consistently the bottleneck, sustaining $86$--$90\%$ average
utilisation across all runs, with peaks reaching near $100\%$. CPU utilisation
remained moderate ($\approx 29\%$ average), confirming that the pipeline's
computational load is predominantly on the GPU as expected for a
TensorRT-accelerated inference workload. RAM usage was stable at $70$--$73\%$,
leaving sufficient headroom for the operating system and other processes.

Temperatures remained well within the $85\degree\mathrm{C}$ throttling
threshold, peaking at $65\degree\mathrm{C}$ during the longest run (IITP20
medium, $41$ minutes). The absence of any thermal throttling across nearly $93$
minutes of cumulative processing confirms that the Jetson Orin Nano can sustain
the pipeline workload without performance degradation with the default cooling
solution.

The hardware utilisation results indicate that the pipeline is computationally
demanding but manageable within the constraints of the Jetson Orin Nano, with
the GPU being the primary resource under load. The system's thermal performance
is sufficient to maintain consistent processing without throttling, even during
extended runs on complex video sequences. However, the high GPU utilisation
suggests that there is limited headroom for additional computational tasks or
more complex models without further optimisations. A potential consideration
could be to explore stronger quantisation (e.g., INT8) or model pruning to
reduce the computational load and increase headroom for future enhancements.